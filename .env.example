# =============================================================================
# ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and update with your values
# 
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# SECURITY KEYS (REQUIRED)
# -----------------------------------------------------------------------------
# These are used by Directus for encryption and session management.
# 
# HOW TO GENERATE:
# Run these commands in your terminal to generate secure random keys:
#   openssl rand -base64 32
#   openssl rand -base64 32
# 
# Copy the output and paste below (run the command twice for two different keys)
# 
# Example output: "sVgdl4yNDDcjD3Pdc8h9J+uKaeyYKiwoEEOD2RWWKg8="
# -----------------------------------------------------------------------------
KEY=your-random-32-char-base64-key-here
SECRET=your-random-32-char-base64-secret-here

# -----------------------------------------------------------------------------
# DIRECTUS ADMIN CREDENTIALS
# -----------------------------------------------------------------------------
# These credentials are used to create the initial admin user.
# You can change these after first login via the Directus admin panel.
# 
# FOR PRODUCTION: Use a strong, unique password!
# -----------------------------------------------------------------------------
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# PostgreSQL database settings (used by both Directus and Docker Compose)
# 
# NOTE: When using Docker Compose, these values must match in both services.
# Default values work out of the box for local development.
# -----------------------------------------------------------------------------
DB_DATABASE=directus
DB_USER=directus
DB_PASSWORD=directus

# -----------------------------------------------------------------------------
# DIRECTUS PUBLIC URL
# -----------------------------------------------------------------------------
# The URL where Directus is accessible.
# Astro will fetch content from this URL during build.
# 
# Local development: http://localhost:8055
# Production: https://cms.yourdomain.com
# -----------------------------------------------------------------------------
PUBLIC_DIRECTUS_URL=http://localhost:8055

# =============================================================================
# QUICK START
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Generate keys: openssl rand -base64 32 (run twice)
# 3. Paste the keys into KEY and SECRET above
# 4. (Optional) Change ADMIN_EMAIL and ADMIN_PASSWORD
# 5. Start services: yarn dev:all
# =============================================================================

# =============================================================================
# PRODUCTION DEPLOYMENT
# =============================================================================
# Before deploying to production:
# 
# 1. Generate NEW secure keys (never reuse development keys!)
#    openssl rand -base64 32
# 
# 2. Set a STRONG admin password (min 16 characters, mixed case, numbers, symbols)
#    Example: Use a password manager to generate
# 
# 3. Update PUBLIC_DIRECTUS_URL to your production domain
#    Example: https://cms.mysite.com
# 
# 4. Use strong database credentials (change DB_USER and DB_PASSWORD)
# 
# 5. Consider using environment-specific .env files:
#    - .env.development (local dev)
#    - .env.production (production)
# =============================================================================
